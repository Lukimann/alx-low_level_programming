#include <stdio.h>

/**
 * main - function to print name
 * @argc: argc parameter
 * @argv: an array
 * Return: Always 0 (success)
 */

int main (int argc, char *argv[])
{
	printf("%s\n", *arg);
	return (0);
}
